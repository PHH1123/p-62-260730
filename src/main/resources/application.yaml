spring:
  application:
    name: p-62-260730
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./db_dev;MODE=MySQL
    username: sa
    password:
  h2:
    console:
      enabled: true

server:
  port: 8080
